# WeMoov Production Environment Variables
# Copy this file to .env.production and fill in your actual values

# ===========================================
# REQUIRED VARIABLES FOR VERCEL DEPLOYMENT
# ===========================================

# Database Configuration (REQUIRED)
# Get this from your PostgreSQL provider (Supabase, Railway, Neon, etc.)
DATABASE_URL="postgresql://username:password@host:port/database?schema=public&connection_limit=5&pool_timeout=20"

# JWT Configuration (REQUIRED)
# Generate a strong secret key (at least 32 characters)
JWT_SECRET="your-super-secret-jwt-key-minimum-32-characters-long"
JWT_EXPIRES_IN="7d"

# Application URLs (REQUIRED)
# Replace 'your-app' with your actual Vercel app name
FRONTEND_URL="https://your-app.vercel.app"
BACKEND_URL="https://your-app.vercel.app"
CORS_ORIGIN="https://your-app.vercel.app"

# Environment (REQUIRED)
NODE_ENV="production"

# ===========================================
# OPTIONAL VARIABLES (for full functionality)
# ===========================================

# Mapbox Configuration (for maps and geocoding)
# Get your token from https://account.mapbox.com/access-tokens/
MAPBOX_ACCESS_TOKEN="pk.your-mapbox-public-token"

# Wave Money API (Senegal mobile payments)
# Contact Wave for API credentials
WAVE_API_KEY="your-wave-api-key"
WAVE_API_SECRET="your-wave-api-secret"
WAVE_WEBHOOK_SECRET="your-wave-webhook-secret"

# Orange Money API (Senegal mobile payments)
# Contact Orange for API credentials
ORANGE_API_KEY="your-orange-api-key"
ORANGE_API_SECRET="your-orange-api-secret"

# Twilio Configuration (SMS notifications)
# Get credentials from https://console.twilio.com/
TWILIO_ACCOUNT_SID="ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
TWILIO_AUTH_TOKEN="your-twilio-auth-token"
TWILIO_PHONE_NUMBER="+221xxxxxxxxx"

# Email Configuration (optional notifications)
# Use Gmail App Password or other SMTP provider
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-specific-password"

# Security Configuration (optional overrides)
BCRYPT_ROUNDS="12"
RATE_LIMIT_WINDOW_MS="900000"
RATE_LIMIT_MAX_REQUESTS="100"

# ===========================================
# VERCEL-SPECIFIC NOTES
# ===========================================

# 1. Add these variables in your Vercel dashboard:
#    Project Settings > Environment Variables

# 2. For DATABASE_URL, ensure your database:
#    - Allows external connections
#    - Has SSL enabled (recommended)
#    - Uses connection pooling

# 3. For security:
#    - Never commit this file with real values
#    - Use strong, unique passwords
#    - Rotate secrets regularly

# 4. Test your configuration:
#    - Deploy to Vercel
#    - Check /api/health endpoint
#    - Verify database connectivity
#    - Test admin login